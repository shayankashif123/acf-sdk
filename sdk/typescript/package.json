{
  "name": "@acf-sdk/acf",
  "version": "0.1.0",
  "description": "Agentic Cognitive Firewall SDK — TypeScript client (v2, deferred)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test dist/tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "typescript": "^5.9.3"
  }
}
